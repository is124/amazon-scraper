# birbal-scraper

To operate:

1. Add a Virtual Environment ```python3 -m venv .venv```
2. Activate the Virtual Environment ```source .venv/bin/activate```
3. Install Requirements ```pip3 install -r requirements.txt```
4. run ```python3 amazon.py```
